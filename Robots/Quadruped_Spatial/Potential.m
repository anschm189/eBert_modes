function pot = Potential(q)
% q is a symbolic vector of the tool coordinates!

% % segmented leg
% l = 0.12;
% r0 = 2*l*cosd(25);
% k = 3.0; % SEA joint stiffness
% b_sh_vec = [0.165; 0.09; 0.02];
% shx_offset = 0.03; %
% qj0_leg = 0.5*acos(r0^2/(2*l^2)-1);
% pot = Ux_fcn(q, b_sh_vec, shx_offset, l,  qj0_leg, k, r0);

% numerical
% handled separately.


end